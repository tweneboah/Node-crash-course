<style>
  .hero-section {
    background: var(--gradient);
    height: 100vh;
    color: white;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
  }

  .hero-section::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="40" fill="rgba(255,255,255,0.1)" /></svg>')
      repeat;
    opacity: 0.1;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    opacity: 0.9;
    margin-bottom: 2rem;
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    padding: 1rem 2.5rem;
    font-size: 1.2rem;
    font-weight: 600;
    background: white;
    color: var(--primary-color);
    border-radius: 50px;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    position: relative;
    overflow: hidden;
    z-index: 1;
  }

  .hero-cta::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--gradient);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }

  .hero-cta:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
    color: white;
  }

  .hero-cta:hover::before {
    opacity: 1;
  }

  .hero-cta i {
    margin-right: 0.75rem;
    font-size: 1.4rem;
  }

  .feature-card {
    background: white;
    border-radius: 15px;
    padding: 2rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    transition: transform 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-5px);
  }

  .feature-icon {
    font-size: 2.5rem;
    color: var(--primary-color);
    margin-bottom: 1rem;
  }

  .cta-section {
    background: white;
    padding: 80px 0;
  }

  .hero-icons {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
  }

  .hero-icon {
    font-size: 8rem;
    color: rgba(255, 255, 255, 0.9);
    margin: 0 1rem;
    animation: float 3s ease-in-out infinite;
  }

  .hero-icon:nth-child(2) {
    animation-delay: 0.5s;
  }

  .hero-icon:nth-child(3) {
    animation-delay: 1s;
  }

  @keyframes float {
    0% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
    100% {
      transform: translateY(0px);
    }
  }

  .feature-card-accent {
    border-top: 4px solid var(--accent-color);
  }
</style>

<!-- Hero Section -->
<section class="hero-section">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-lg-6">
        <h1 class="hero-title">Never Miss an Important Email Again</h1>
        <p class="hero-subtitle">
          Schedule email reminders and stay on top of your important
          communications. Our intelligent system ensures you never forget to
          follow up.
        </p>
        <a href="/schedule" class="hero-cta">
          <i class="bi bi-calendar-plus"></i>Schedule a Reminder
        </a>
      </div>
      <div class="col-lg-6">
        <div class="hero-icons">
          <i class="bi bi-envelope-paper hero-icon"></i>
          <i class="bi bi-alarm hero-icon"></i>
          <i class="bi bi-check2-circle hero-icon"></i>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Features Section -->
<section id="features" class="py-5">
  <div class="container">
    <div class="row text-center mb-5">
      <div class="col-12">
        <h2 class="display-4">Why Choose Our Email Reminder Service?</h2>
      </div>
    </div>
    <div class="row g-4">
      <div class="col-md-4">
        <div class="feature-card text-center feature-card-accent">
          <i class="bi bi-clock-history feature-icon"></i>
          <h3>Smart Scheduling</h3>
          <p>
            Schedule reminders for any date and time, with support for recurring
            reminders.
          </p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="feature-card text-center feature-card-accent">
          <i class="bi bi-shield-lock feature-icon"></i>
          <h3>Secure & Private</h3>
          <p>
            Your data is encrypted and protected. We never share your
            information.
          </p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="feature-card text-center feature-card-accent">
          <i class="bi bi-bell-fill feature-icon"></i>
          <h3>Instant Notifications</h3>
          <p>
            Get notified instantly when it's time to follow up on your emails.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="cta-section">
  <div class="container text-center">
    <h2 class="display-5 mb-4">Ready to Get Started?</h2>
    <p class="lead mb-4">
      Schedule your first reminder now and never miss an important email again.
    </p>
    <a href="/schedule" class="btn btn-primary btn-lg">
      <i class="bi bi-calendar-plus me-2"></i>Schedule a Reminder
    </a>
  </div>
</section>
